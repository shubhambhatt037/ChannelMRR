import type { ConfigProps } from "@/types/config";

const config: ConfigProps = {
	appName: "ChannelMRR",
	appTagline: "The marketing channels behind verified startup revenue",
	appDescription:
		"Discover which marketing channels real startups use, ranked by frequency. Filter by revenue, market, audience, founder. Powered by verified TrustMRR data.",
	domainName: "channelmrr.com",
};

export default config;
